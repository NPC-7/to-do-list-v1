<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>To Do List App</title>
</head>
<body>

<% if (kindOfDay === "Saturday" || kindOfDay === "Sunday") { %>
    <h1 style="color: purple">It's a <%= kindOfDay %> To Do List</h1>
<% } else { %>
    <h1 style="color: blue">It's a <%= kindOfDay %> To Do List</h1>
<% } %>


</body>
</html>
